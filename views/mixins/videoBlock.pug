mixin videoBlock(video={})
    .videoBlock
      video.videoBlock__thumbnail(src=video.videoFile, controls=true, autoplay=true)
      a(href=routes.videoDetail(video.id))
        h4.videoBlock__title=video.title
        h6.bideoBlock__views=video.views
    
